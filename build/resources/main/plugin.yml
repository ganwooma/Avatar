name: PlayerCorpsePlugin
version: '1.0'
main: com.ganwooma.playerCorpsePlugin.PlayerCorpsePlugin
api-version: '1.20'
authors: [Ganwooma]

commands:
  corpsereload:
    description: Reloads the PlayerCorpsePlugin and removes all existing corpses
    usage: /corpsereload
    permission: playercorpse.admin
    permission-message: §cYou do not have permission to use this command.

permissions:
  playercorpse.admin:
    description: Allows access to administration commands
    default: op